// import { html } from "lit";

describe("fui-button", () => {
  test.todo("works in multi-window scenarios", () => {
    // ensure document.create* is working in multi-window
  });
  test.todo("updates styles when attributes change");

  describe("event listeners", () => {
    test("adds event listeners", () => {});
    test.todo("removes event listeners");
    test.todo("keeps event listeners when tag name changes");
  });
});
